<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tteqr">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_SMS" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@drawable/tte_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TTEQR">
        <activity android:name=".TicketActivity"></activity>
        <activity android:name=".PaymentActivity" />
        <activity android:name=".DetailsActivity" />
        <activity android:name=".TicketDetailsActivity" />
        <activity android:name=".CheckInActivity" />
        <activity
            android:name=".CapturePortrait"
            android:screenOrientation="sensorPortrait"
            android:stateNotNeeded="true"
            android:theme="@style/zxing_CaptureTheme"
            android:windowSoftInputMode="stateAlwaysHidden" />
        <activity android:name=".PassengerDetailActivity" />
        <activity android:name=".TrainVerificationActivity" />
        <activity android:name=".BookingActivity" />
        <activity android:name=".VerificationActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MobileActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>